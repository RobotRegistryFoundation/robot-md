# P66 Conformance Checklist — robot-md#96 Phase 0

**What this is.** The Phase 0 deliverable of [robot-md#96] ("Always-on RCAN + Protocol 66 for every robot — prove end-to-end vs RRF"). It is the invariant-by-invariant conformance matrix across the four components, and it is the test plan for Phases 1–3. Every row states what we found, where we found it (file:line), and what test proves it — or the one-line plan for the test that would. **This doc never asserts**: a row only says `implemented` when it carries a real citation.

**Judged against.** rcan-spec P66 as published at rcan.dev (repo `/home/craigm26/rcan-spec`, HEAD `e9899ed`). The normative P66 content landed in the CHANGELOG v1.5.0/v1.6.0 entries (CHANGELOG.md:92–223); the repo has since moved on (CHANGELOG head 2.3.0, package.json 3.0.0, P66 manifest examples self-describe `rcan_version: 3.2`) but the v1.5/v1.6 sections remain the authoritative section-to-gap mapping for P66. Known version-numbering quirks are noted in the rcan-spec table.

**As of 2026-06-10.** Status reflects **merged main** of each repo:

| Component | Judged at | Caveat |
|---|---|---|
| rcan-spec | `e9899ed` (`/home/craigm26/rcan-spec`) | "implemented" in this table means *normative spec text exists*, not that any code enforces it |
| robot-md-gateway | origin/main `9bb5529` (`/home/craigm26/robot-md-gateway`) | the **deployed** venv on Bob (`/opt/robot-md-gateway/.venv`, 0.5.0a4) is a pre-attestation build that lags origin/main, and origin/main *also* says 0.5.0a4 — builds are indistinguishable by version string; deployed-state divergences are called out in evidence |
| robot-md | origin/main `4c7aa9b` (worktree `/home/craigm26/rm-conformance`) | branch `feat/commission-bringup` carries doctor/commission pieces — **in-flight, not counted** |
| OpenCastor | HEAD `38ff96b` (`/home/craigm26/OpenCastor`, 2026-06-09 nightly) | OpenCastor is the designated P66 tier but is **not** wired into Bob's current gated stack (Bob's enforced path is robot-md-gateway) |

In-flight branches and open PRs are noted inline as *in-flight* and never counted as implemented. Status values: `implemented` / `partial` / `missing` / `unknown`.

---

## 1. rcan-spec — normative completeness

"Implemented" here = the published spec carries normative text for the invariant. "Partial" = the text exists but contradicts itself, lacks a machine-checkable artifact, or lacks normative force. "Missing" = the epic requires it and no spec text exists. Conformance-suite citations refer to `src/pages/conformance/index.astro` (the published L1–L4 suite).

| Invariant (spec citation) | Status | Evidence | Proving test |
|---|---|---|---|
| **estop-never-blocked** — ESTOP skips queues, exempt from replay rejection (fresh ≤10s processed even on dup msg_id), LoA-exempt, accepted from revoked robots, accepted cross-registry pre-trust-chain, accepted offline from same-network owner w/ cached key, never rate-limited (safety.astro; replay-prevention; identity; revocation; federation; offline) | implemented | `src/pages/docs/safety.astro:66-70`; `replay-prevention.astro:62-70`; `identity.astro:288`; `revocation.astro:74-82`; `federation.astro:238-246,367`; `offline.astro:14-16,46-50`; `docs/v1.5-implementation-plan.md:1009-1016` | No conformance test for the non-blocking property. PLAN: L2 row "fresh ESTOP accepted under each adverse condition: duplicate msg_id, revoked source RRN, LoA-1 JWT, offline_mode=true, federation_enabled=false" |
| **local-safety-wins** — required invariant #1 of the 5 P66 invariants; no remote command bypasses on-device safety/bounds/e-stop (safety.astro §6; spec/v1.1 §6 item 1; federation §18.1) | partial | `safety.astro:58-63,100-101,119`; `spec/v1.1.astro:477`; `federation.astro:15,170`. **Contradiction:** safety.astro:119 claims `local_safety_wins` is schema-required since v1.4, but `schemas/rcan-config.json` declares `safety.required=[]` — a config omitting it validates. Asserted, not provable | PLAN: schema test that a config omitting `safety.local_safety_wins` FAILS validation (currently would pass — fix schema, then vitest in `tests/`) |
| **cloud-commands-same-gates** — cloud commands traverse the identical confidence gates + bounds checks as local; gates fire at dispatch before transport (README.md:43; safety landing; §8.5 sender_type) | partial | `README.md:43`; `src/pages/safety.astro:98,112-115`; cloud-relay §8.5 (`CHANGELOG.md:177`); `spec/v1.1.astro:477`. GAP: principle only — no normative "cloud-relayed commands MUST traverse the identical gate set" section, no gate-parity-by-ingress conformance test | PLAN: conformance test "same low-confidence command blocked identically via (a) local HTTP, (b) cloud relay with sender_type=cloud_function" — add to L2 §16.2 rows |
| **confidence-gates-immutable** — `confidence_gate.*` are compile-time constants; CONFIG_UPDATE containing them MUST be rejected regardless of JWT role (§9.2) | implemented | `messages.astro:358-360,369`; `docs/v1.5-implementation-plan.md:1022,389`; `schemas/rcan-config.json` agent.confidence_gates | Gating: L2-CONF-001/002 (`conformance/index.astro:175-185`). Immutability untested — PLAN: "CONFIG_UPDATE payload containing confidence_gate.* rejected with audit event" |
| **estop-explicit-clear** — stays stopped until authenticated ESTOP_CLEAR; no auto-recovery; RESUME from revoked robot rejected; cross-registry RESUME needs consent + trust chain + local owner ack; §8.9 presence MAY gate clear | implemented | `safety.astro:73-77`; `revocation.astro:82`; `federation.astro:245`; `CHANGELOG.md:191` (§8.9); `delegation.astro` | L2-SAFE-001 latch (`conformance/index.astro:150-155`). Clear-auth-strength untested — PLAN: "ESTOP_CLEAR from sub-owner / without presence token when required → 403" |
| **ai-cannot-override-safety** — AI brain sandboxed; cannot modify safety envelope, disable proximity, or alter ESTOP state; safety.* + brain.provider config require creator role (§9.2) | implemented | `safety.astro:80-84`; `messages.astro:348-356`; `docs/v1.5-implementation-plan.md:1020` | GAP-07 test requirements exist only in the plan (`v1.5-implementation-plan.md:395-400`). PLAN: promote to published L2 rows |
| **audit-trail-complete** — every safety event to HMAC-chained tamper-evident log; all COMMAND/CONFIG logged w/ principal/RURI/ts/msg_id/outcome; §20 signed JSONL export, offline verification | implemented | `safety.astro:87-91,346-381`; `spec/v1.1.astro:479`; `spec/audit-bundle-v1.md:1-69` | L2-HMAC-001 + L3-CHAIN-001 (`conformance/index.astro:169-173,232-237`) — exists |
| **p66-manifest-endpoint** — GET /api/safety/manifest MUST return protocol:66, the 5 invariant booleans, hardware_safety, envelope, clock/offline/audit fields, and v1.6 fields (min_loa_for_control, federation_enabled, trusted_registries, supported_transports) | partial | `safety.astro:16-50,323-334,375-381,384-453` (complete v1.6 example); `CHANGELOG.md:147,118,130,142`. **GAP (epic gap 4 root):** no machine-validatable JSON Schema — `schemas/` has rcan-config, messages, audit-bundle but **no safety-manifest.json** (verified by ls); `p66_conformance_pct` appears in TRANSPARENCY payloads with no normative definition | PLAN: publish `schemas/safety-manifest.json` + vitest validating the v1.6 example in safety.astro against it; then robot-md/gateway can validate live manifests instead of asserting a boolean |
| **clock-sync-restricted-mode** — §8.4 (v1.5 MUST, GAP-04): NTP/NTS before operational mode, ±5s drift; clock_synchronized=false → restricted mode (same-network same-owner only, WARNING audit/60s); ESTOP always accepted | implemented | `safety.astro:311-343`; `CHANGELOG.md:176`; `docs/gap-analysis-v1.5.md:80-93` | None in conformance suite. PLAN: "clock_synchronized=false → cross-owner rejected, same-owner accepted, ESTOP accepted" |
| **offline-mode-local-key-cache** — §14 (v1.5 MUST, GAP-06): offline after offline_grace_s (300s); JWKS cache `~/.rcan/key_cache.json` (24h TTL, OFFLINE_KEY_CACHE_STALE past it); 6-step offline JWT validation; cross-owner blocked after 3600s; manifest reports offline_mode + offline_since_s; offline ESTOP from same-network owner ALWAYS accepted | implemented | `offline.astro:11-146`; `CHANGELOG.md:184`; `gap-analysis-v1.5.md:114-127`; `v1.5-implementation-plan.md:336-364,1015`. Naming drift: epic's `offline_grace_period_s` = the page's `offline.grace_period_s` (86400, key-cache validity), distinct from `offline_grace_s` (300, trigger) — `offline.astro:129-132` | Acceptance only in plan (`v1.5-implementation-plan.md:364`). PLAN: L2 rows "registry down: owner JWT + cached key accepted; cross-owner rejected after 3600s; stale cache → OFFLINE_KEY_CACHE_STALE; ESTOP accepted throughout" |
| **fleet-estop-500ms-sla** — §15 (v1.5 SHOULD, GAP-13): FLEET_COMMAND (type 23, QoS 1); UDP multicast 239.255.66.0/24:6600, TCP unicast fallback, cloud fanout; fleet ESTOP MUST reach all members of a ≤100-robot fleet within 500ms or fall back to parallel unicast | partial | `messages.astro:250-256,378-409`; `CHANGELOG.md:189`; `v1.5-implementation-plan.md:516-543`. Underspecified: SHOULD-only; no wire encoding for the multicast frame; no group-address selection rule; FLEET_COMMAND_RESULT only in the plan; no normative 500ms measurement method | Plan-only (`v1.5-implementation-plan.md:538`). PLAN: multi-robot conformance harness measuring multicast ESTOP receipt latency at n=10/100 + unicast-fallback correctness |
| **estop-qos2-exactly-once** — §5.3 (v1.5 MUST, GAP-11): ESTOP QoS 2; processed on receipt (ACK is confirmation, never precondition); SAFETY msgs QoS ≥1; ACK-timeout after max retries → local safety halt; duplicate ESTOP no-op but still ACKed | implemented | `qos.astro:15,39,48,82,104-116`; `CHANGELOG.md:183`; `gap-analysis-v1.5.md:199-212`; `v1.5-implementation-plan.md:480-512,1014` | Plan-only (`v1.5-implementation-plan.md:504-510`). PLAN: add to published L2 table |
| **min-loa-for-control** — §8.7 (v1.6, GAP-14): manifest field (1–3, default 1); reject control-scope commands below it; enforced from cached JWT loa offline; ESTOP LoA-exempt; community registries issue LoA 1 only | implemented | `identity.astro:15,104,135,200-292`; `safety.astro:399-406`; `CHANGELOG.md:108-118`. Minor: `min_loa_for_safety` only in pseudocode (identity.astro:249), never a declared manifest field | None published. PLAN: "control w/ loa=1 vs min_loa_for_control=2 → rejected; ESTOP w/ loa=1 → accepted; same with offline_mode=true" |
| **federation-never-weakens-safety** — §18 (v1.6, GAP-16): federation_enabled default false → reject ALL cross-registry commands incl. STATUS; trusted_registries = explicit allowlist; cross-registry ESTOP accepted pre-verification + logged; RESUME needs verified consent + trust chain + local owner ack | implemented | `federation.astro:15,165-170,238-246,301-319,367`; `CHANGELOG.md:120-130`; `safety.astro:408-414` | None published. PLAN: "federation_enabled=false: cross-registry STATUS rejected, cross-registry ESTOP accepted+logged; RESUME without local owner ack rejected" |
| **estop-always-fits-constrained-transport** — §19 (v1.6, GAP-17): supported_transports[]; 'minimal' = fixed 32-byte binary frame, ESTOP+ACK only; "ESTOP always fits" one LoRa SF12 frame; Compact ESTOP <200B | implemented | `constrained-transport.astro:15,34-44,101-106`; `safety.astro:417-422`; `CHANGELOG.md:132-142` | None published. PLAN: byte-level golden test of the 32-byte ESTOP frame encode/decode + Compact <200B size assertion |
| **graceful-degradation-watchdog** — §6 inv 2 + SOFTWARE_001: network loss → safe-stop within one latency_budget_ms; missing brain heartbeat for watchdog.timeout_s → WATCHDOG_TIMEOUT, actuators safe, ESTOP state, operator clear required | implemented | `spec/v1.1.astro:478`; `safety.astro:211-251` | L2-SAFE-002 (`conformance/index.astro:156-161`) — exists |
| **safety-priority-queue-bypass** — Priority 3 (SAFETY) bypasses all queues; a safety command can never be delayed by a backlogged queue; ESTOP never rate-limited | implemented | `messages.astro:327`; `safety.astro:66-70`; `v1.5-implementation-plan.md:1011` | None published. PLAN: "ESTOP processed ahead of N-deep queued COMMANDs; ESTOP not subject to role rate limit" |
| **mdns-local-supremacy** — `_rcan._tcp.local` discovery works even if cloud registry deletes the robot; local peers discoverable regardless of registry state; OWNER can redirect upstream without losing local discovery | implemented | `discovery.astro:96`; `spec/v1.0.astro:219`; `spec/v1.1.astro:500-505`; `v1.5-implementation-plan.md:1021` | None published (multi-actor mDNS untested — epic gap 8). PLAN: two-node test: registry unreachable → peer still resolvable via mDNS and accepts owner ESTOP |
| **gating-liveness-never-ungated** — EPIC-REQUIRED, NOT IN SPEC: nothing requires the enforcement layer be continuously alive, that loss of the gating *process* (vs network) trigger safe-stop, or that gating liveness be externally attestable. The P66 manifest is self-reported by the very process that can be silently stopped | missing | Searched `src/pages/**/*.astro`, `schemas/`, `docs/` for gating/liveness/always-on/gateway: only HEARTBEAT ping (`messages.astro:120`); §6 degradation covers NETWORK loss only (`spec/v1.1.astro:478`); gateway JWTs (`v1.1.astro:447-452`) are auth-only. Nothing binds "gateway down" to "safe-stop" or "fleet alarm" | PLAN: new spec section (§6 invariant or P66 manifest fields `gating_active` + `gating_attestation` w/ signed freshness) + conformance test "kill enforcement process → robot safe-stops within watchdog window AND fleet surface flags robot ungated" |
| **signed-outcome-receipts** — EPIC-REQUIRED (gateway#27), UNDERSPECIFIED: spec has after-the-fact primitives (audit outcome, HMAC chain, §20 signed export, audit-bundle replay) but no per-command signed receipt binding msg_id → outcome → chain position at execution time | partial | `spec/v1.1.astro:479`; `safety.astro:346-381` (batch export only); `spec/audit-bundle-v1.md:5-29`; `schemas/commitment.json`. No "receipt" concept anywhere (grep across src/pages) | PLAN: spec a RECEIPT field on RESPONSE/COMMAND_ACK (sig over canonical {msg_id, outcome, chain_hash, ts}) + conformance test "every executed/blocked command returns a verifiable signed receipt linking into the exported audit chain" |
| **manifest-provenance-no-drift** — EPIC-REQUIRED (gap 5), NOT IN SPEC: §8 validates config at startup only; §9.2 config_hash covers CONFIG_UPDATE payloads only; nothing requires the running/enforced config to be provably identical to the registry-anchored canonical manifest; no manifest-hash in P66 manifest; no drift alarm | missing | `spec/v1.1.astro:511-540`; `messages.astro:338-372`; searched safety/offline/identity .astro for manifest-hash/provenance/drift: absent. Nearest hook: audit-bundle version-tuple artifacts (point-in-time only) | PLAN: add `enforced_config_hash` (+ source URI) to the P66 manifest; spec "MUST equal hash of registry-resolved canonical manifest or robot enters restricted mode"; conformance test mutates the enforced copy → restricted mode + audit event |
| **hitl-gates** — §16.3: gated action_type MUST NOT dispatch immediately (queued_for_hitl); AUTHORIZE by OWNER, rejected for GUEST; gates enforced at dispatch as protocol invariants. Multi-actor behavior (fleet/delegation) unspecified beyond §2.7 non-escalation | partial | `src/pages/safety.astro:12-31,120-135`; `conformance/index.astro:214-225`; `CHANGELOG.md:64-67`. No HITL semantics in §15 FLEET_COMMAND (`messages.astro:378-409`) | L3-HITL-001/002 exist (single-robot). PLAN: multi-actor test "FLEET_COMMAND matching a member robot's HiTL gate is queued per-robot, not auto-dispatched" |

**rcan-spec internal hygiene to fix alongside Phase 2** (found while auditing, not invariants): (a) safety.astro:119 vs `schemas/rcan-config.json` safety.required=[] contradiction (above); (b) README's Spec Sections table (README.md:64-65) still describes legacy §14 Provider Management / §15 Swarm Coordination while the v1.5 docs pages define §14 Offline Operation / §15 Fleet Broadcast — two competing §14/§15 meanings in one repo; (c) the tightest P66 invariant list anywhere ("Safety Invariants (Non-Negotiable)", `docs/v1.5-implementation-plan.md:1007-1022`) is not mirrored into the published spec — worth promoting; (d) repo tests (`tests/functions.test.ts`, 101 vitest) cover RURI/node-manifest only — zero P66 invariants are executable tests in the spec repo; (e) the published L1–L4 conformance suite contains zero fleet-multicast, federation, offline-ESTOP, or LoA tests.

---

## 2. robot-md-gateway

Judged at origin/main `9bb5529`. Headline: on Bob today, the enforced gate set is exactly **envelope-sig (vs prod RRF) + replay (in-memory) + manifest provenance + tier + tool allowlist**. SafetyMonitor, ConfidencePolicy, and RevocationResolver are implemented + unit-tested but **unreachable from the `serve` entrypoint** (`__main__.py:226-263` builds make_app without them), and the deployed `gateway.env` leaves HITL/RRN-binding/attestation flags unset.

| Invariant | Status | Evidence | Proving test |
|---|---|---|---|
| **envelope-sig-vs-prod-rrf** — every actuation envelope Ed25519-verified against keys resolved from prod RRF /v2/keys before any policy/actuation (RC-001) | implemented | `src/robot_md_gateway/cert/envelope.py:33-69`; `auth.py:158-198` (RRFResolverFromEnv → `{RRF_URL}/v2/keys/{kid}`); `receiver.py:362-376` (deny 403 before parse); live on Bob: `/etc/robot-md-gateway/gateway.env:5` `ROBOT_MD_REQUIRE_ENVELOPE_SIGNATURE=1`; live proof vs prod RRF 2026-05-29 (see §4 baseline) | `tests/cert/test_rc_001_envelope_signature.py:45,57,70` + `tests/cert/test_rc_003_004_receiver_wiring.py` — exists |
| **fail-closed-on-tamper** — tampered/unsigned manifests and envelopes rejected 403 (MF-002 / RC-001) | implemented | `manifest_provenance.py:51-90` (every branch returns accepted=False); `receiver.py:414-436`; proven live 2026-05-29 (tampered/rogue-kid → 403 vs prod RRF) | `tests/cert/test_mf_002_manifest_reject.py:39,54,71`; `test_rc_001_envelope_signature.py:70` — exists |
| **estop-never-blocked** — safety state preempts all gates; an e-stop command must always have a path through the gated channel | partial | Preemption implemented+tested: `cert/safety.py:24-33,57`; `receiver.py:348-360` (safety check FIRST). BUT (a) production serve never wires a SafetyMonitor (`__main__.py:226-238,251-263`) — deployed gateway has no ESTOP state input; (b) **no estop tool on Bob's allowlist** (`/etc/robot-md-gateway/gateway.env:4`: move/home/read_state/paced_move/raw_tick_move/commission_probe/set_torque) — under full gating there is NO e-stop path through the gateway (software estop is via ungated robot-md CLI); (c) legacy dispatch denies read-tier estop (`tests/test_gating.py:76-84`); ESTOP scope exempt from tiers (`cert/policy.py:41`), confidence threshold 0.50 (`cert/gates.py:17`) — neither reachable in prod | `tests/cert/test_phase_4_safety_wiring.py:123-135` (in-process precedence). MISSING: serve-level test that production entrypoint constructs+wires SafetyMonitor; HIL SF-001 latency run per `docs/hil/track-3-test-plan.md` (task #23, never executed) |
| **safe-stop-on-network-loss** — heartbeat staleness (3s) → SAFE_STOP, blocks actuation, no auto-resume (SF-002) | partial | `cert/safety.py:35-55`; per-request tick `receiver.py:348-350`. Not wired in production serve; nothing ever calls on_heartbeat in prod — no heartbeat source | `tests/cert/test_sf_002_network_loss.py:8,16,22` + `test_phase_4_safety_wiring.py:94-108`. MISSING: HIL SF-002 (track-3 plan: 10 drop events → SAFE_STOP <3.5s) |
| **cloud-cmds-same-confidence-gates** — cloud/remote commands pass the same confidence gates as local (RC-003) | partial | Gate implemented default-strict: `cert/gates.py:11-56` (missing confidence → deny); wiring `receiver.py:491-505`. BUT serve never passes confidence_policy, no env flag exists → **OFF in the deployed gateway** | `tests/cert/test_rc_003_confidence.py:6-24` + receiver wiring tests. MISSING: env-flag wiring test (ROBOT_MD_CONFIDENCE_*) proving the production entrypoint enforces it |
| **hitl-gates-enforced-under-rcan** — manifest-declared safety.hitl_gates gate MANIPULATE/EXECUTE envelopes (RC-004 + hitl_from_manifest) | partial | `cert/gates.py:60-160` (destructive→MANIPULATE, system→EXECUTE/ACTUATE); `receiver.py:507-529`; env flag `__main__.py:40-47`. BUT Bob's gateway.env does NOT set ROBOT_MD_HITL_FROM_MANIFEST → **HITL gate OFF in production**; the manifest's declared destructive/system gates are unenforced by the gateway today | `tests/cert/test_rc_004_hitl.py:6-31` + `test_hitl_from_manifest.py:9-38`. MISSING: deployed-config conformance check (doctor/commission probe failing when manifest declares hitl_gates but the gateway flag is off) |
| **replay-protection** — replayed envelopes rejected (RC-002) | partial | `cert/envelope.py:96-135`, active when sigs required (prod: yes). Weak: in-memory only — restart clears seen-set; msg_id-only, no timestamp/nonce expiry; arbitrary eviction at max_size; docstring says production needs sqlite/redis (`envelope.py:97-100`) | `tests/cert/test_rc_002_replay_protection.py`. MISSING: persistence-across-restart test + HIL tail-replay (`hil/run.py:228-234` PHASE-5 REPLAY mode exists, unexecuted) |
| **outcome-receipts (gateway#27)** — every allow AND deny produces a gateway-signed outcome receipt verifiable downstream | partial | At origin/main (PRs #24/#25/#26, **NOT deployed**): `attestation.py` (SigningIdentity :34, build_outcome :126, build_action_trace :109); `receiver.py:181-309` (_emit_attestation on allow + every deny); `shipper.py:1-80`. Deployed 0.5.0a4 venv has NO attestation.py/shipper.py (verified absent) and gateway.env lacks ROBOT_MD_ATTESTATION_*; the /v1/invoke RESPONSE still carries no signed receipt (`receiver.py:588-596` plain dict) — **gateway#27 still OPEN** (verified via gh) | `tests/test_receiver_attestation.py:76-190`; `tests/test_interop_gate.py:26-75`; `tests/test_shipper.py:28-105`. MISSING: deploy + signed-receipt-in-response test |
| **actuator-gated-path-parity (gateway#28)** — every registered actuator (incl. camera) invocable through the gated path with the same Actuator Protocol | missing | Gateway calls `execute(envelope=, manifest_path=, tier=, config=)` (`receiver.py:558-563`; `actuator.py:46,63`). oak-d-actuator 0.2.1 (deployed venv AND its repo main `0903e97`) declares `execute(self, tool_name, tool_args)` (`/home/craigm26/oak-d-actuator/src/oak_d_actuator/actuator.py:52`) → TypeError through the gateway. **gateway#28 OPEN; fix = oak-d-actuator PR #11 (in-flight, unmerged)**. so-arm101-actuator conforms and works gated | MISSING: gateway-side conformance test iterating every `robot_md_gateway.actuators` entry-point asserting the kwargs execute() signature; merge+release oak-d-actuator#11; HIL gated vision_find |
| **always-on-gating-no-silent-stop** — gateway cannot be silently stopped leaving the robot ungated; gating-liveness observable (epic gaps 1+7) | missing | `/etc/systemd/system/robot-md-gateway.service:12` `Restart=on-failure` only — clean `systemctl stop` is permanent and silent (precedent 2026-06-03: Bob ran ungated for days); no WatchdogSec, no liveness heartbeat/alert (grep watchdog/heartbeat-emitter over src+systemd+docs = zero); no fleet surface. Partial mitigation: udev lockout persists when gateway is down — but see device-node-isolation: the operator account bypasses it | MISSING: (a) CI check that the shipped unit has Restart=always + WatchdogSec + sd_notify; (b) liveness heartbeat to a fleet surface w/ alert-on-silence test; (c) HIL: stop the service, prove actuation impossible from every non-gateway UID |
| **device-node-isolation (GW-001)** — direct /dev/ttyACM0 denied to non-gateway UIDs; gateway is the only actuation path | partial | Generator `udev_policy.py:14-40` + `udev/99-robot-md-gateway.rules.in`; live perms `crw-rw---- robot-md-gateway:robot-md-gateway`. **DEFEATED on Bob today:** `id craigm26` shows membership in robot-md-gateway AND dialout → operator UID opens the device directly, ungated; track-3 step 5 (EACCES from operator UID) would FAIL right now | `tests/cert/test_gw_001_device_isolation.py:15-35` (rules text only). MISSING: live HIL GW-001 per `docs/hil/gw-001-procedure.md` — fails until craigm26 is removed from the robot-md-gateway group |
| **manifest-drift-detection** — enforced (/etc/robot-md-gateway/ROBOT.md) and canonical (~/ROBOT.md) cannot silently diverge; gateway pins which manifest governs (epic gap 5) | missing | Live drift confirmed: md5 ece9ed03 (enforced, signed, RRN-011) vs 519bcedd (canonical, NO ROBOT-MD-SIG footer). No drift detection (grep drift over src). Worse: the **ENVELOPE chooses manifest_path** (`receiver.py:108`, verified at :414) — the gateway never pins to ROBOT_MD_PATH (set in gateway.env:1 but unused by the receiver), so any signed manifest on disk can be named; ROBOT_MD_REQUIRE_RRN_BINDING (`__main__.py:50-57`, `cert/rrn_binding.py`) NOT set in deployed env | MISSING: (a) receiver test that manifest_path outside the pinned ROBOT_MD_PATH is rejected (or rrn-binding required by default); (b) doctor/commission check that enforced vs canonical hashes match or drift is surfaced |
| **clock-sync (P66 clock_synchronized)** — gateway verifies/uses synchronized time | missing | Zero hits for clock/ntp/skew/timestamp-freshness across src + systemd + docs (grep 2026-06-10); envelopes carry no verified timestamp; replay is msg_id-only (`cert/envelope.py:118-135`) | MISSING: envelope timestamp-window check + chrony/timedatectl sync probe in doctor/commission, with a stale-clock-denies test |
| **offline-mode (P66 offline_mode)** — defined behavior when RRF unreachable (cached keys, revocation staleness bound) | missing | No offline semantics: RRFResolverFromEnv caches kid→PEM in-memory forever (`auth.py:162-177`) — accidental unbounded offline tolerance for seen keys, fail-closed only for unseen kids; revocation polling RR-001/RR-002 implemented (`cert/revocation.py`) but **NOT wired in serve** → revoked keys keep working in prod | `tests/cert/test_rr_001_rr_002.py` proves the unwired unit. MISSING: serve-wiring test + max-staleness/fail-closed-offline policy test |
| **loa-gates (P66 min_loa_for_control)** — control commands require minimum level-of-authority; nearest analog = read/actuate/commission tiers | partial | Tiers implemented+deployed: `cert/policy.py:41-67`; bearers→tiers wired (`__main__.py:204-208`). But no mapping from P66 min_loa_for_control to tiers; gateway src has ZERO p66 references (grep) while both Bob manifests declare `p66_enabled: true` (`/etc/robot-md-gateway/ROBOT.md:160`, `~/ROBOT.md:301`) — declaration never read or enforced | `tests/cert/test_commission_tier.py:5-31` + `tests/test_gating.py:50-96` prove tiers. MISSING: test that a manifest declaring P66 fields changes gateway enforcement |
| **fleet-multi-actor (P66)** — mDNS, fleet ESTOP UDP 239.255.66.0/24:6600 500ms SLA, federation_enabled, supported_transports | missing | Zero hits for mdns/239.255/6600/federation/multicast across src, docs, systemd, hil (grep 2026-06-10); gateway is single-robot HTTP-only (uvicorn 127.0.0.1:8080) | MISSING entirely: fleet-ESTOP listener + 500ms-SLA latency test would be the proving artifact |
| **audit-evidence-chain (EV-001)** — tamper-evident audit chain over every decision, exportable, offline-verifiable | partial | `cert/audit.py` (hash-chained, Ed25519-signed, offline verifier :82); wired in serve BUT in-memory, restart-wiped by design (`__main__.py:193-195`); only /v1/audit/last exposed (`receiver.py:598-611`) — no full export, no persistence in prod | `tests/cert/test_ev_001_audit_export.py:23-69` + `tests/test_phase_4_audit_wiring.py` + `tests/test_receiver_audit_last.py`. MISSING: persistent chain across restart + full-export surface |

---

## 3. robot-md

Judged at origin/main `4c7aa9b`. Big picture: on main, **P66 is a vocabulary word, not a protocol** — one boolean at schema:356 (tripled across `schema/`, `cli/src/robot_md/schemas/`, `site/`), shipped as `true` in examples and presets, accepted by validate with no corroboration, invisible to doctor, untested by any test. The honest part: SECURITY.md:60 and the Layer-3/4 disclaimer (SECURITY.md:13-16) openly say this repo does not enforce.

| Invariant | Status | Evidence | Proving test |
|---|---|---|---|
| **p66-real-manifest-fields** — ROBOT.md safety block carries the real rcan-spec P66 fields (min_loa_for_control, clock_synchronized, offline_mode, federation_enabled, supported_transports), not a bare boolean (epic gap 4) | missing | `schema/v1/robot.schema.json:356` is the entirety of P66: `"p66_enabled": { "type": "boolean" }` (+ `loa_enforcement` bool :357); mirrored byte-identical at `cli/src/robot_md/schemas/v1/robot.schema.json:356` and `site/schema/v1/robot.schema.json:356`; `spec/robot-md-v1.md:96-97` documents both as 'recommended' bools. Real P66 fields appear nowhere in schema/code (only a stale plan doc). Zero tests reference p66 | PLAN: schema test asserting a `safety.p66` object with the five rcan-spec fields validates, and bare `p66_enabled:true` without them emits a deprecation/warning (extend `cli/tests/test_schema.py`) |
| **p66-claim-not-provable** — a manifest claiming P66-active must be corroborated, not asserted | missing | `examples/bob.ROBOT.md:128`, `examples/turtlebot4.ROBOT.md:40`, preset `cli/src/robot_md/presets/so_arm101.yaml:214` all ship `p66_enabled: true` — every preset robot "claims" P66 with nothing backing it; `validate.py:42-167` accepts it schema-only; no RRF cross-check, no gateway probe, no signature over the safety block (SECURITY.md:62) | PLAN: conformance test that `p66_enabled:true` fails `robot-md doctor --strict` unless a live gateway answers a P66 status probe matching the declared fields |
| **security-md-tier2-enforcement-claim** — SECURITY.md's claim that enforceable limits live at Tier 2 (OpenCastor SafetyLayer + P66 manifest) is verifiable (epic gap 6) | partial | SECURITY.md:60 ('declarative, not enforcing' — honest) but the Tier-2 half is a bare cross-repo pointer: SECURITY.md:49 scopes OpenCastor out; nothing tests/links any SafetyLayer P66 conformance; `verify_tier.py:1-22` promotes RRF verification tiers, never touches P66/safety | PLAN: cross-repo conformance test (robot-md emits a bounds-violating command fixture; OpenCastor SafetyLayer must reject it) published as a Tier-2 badge the manifest can cite |
| **doctor-commission-and-drift-bucket** — doctor surfaces gating/commission state: manifest-sync + commission verification (epic gaps 1, 5) | missing | `doctor.py:7-13` documents five buckets (install, manifest, network, drivers, keystore); `run_all` (doctor.py:407-416) adds check_calibration — no commission bucket, no drift check, no gateway-liveness check on main. Closest: INFO that a serial port is 'claimed by robot-md-gateway user (intentional)' (`doctor.py:276-282`) — explains the claim, does not verify liveness or manifest match. *In-flight: a commission bucket exists on `feat/commission-bringup` (a3c0f95) — not counted* | PLAN: doctor test asserting (a) FAIL when gateway unit installed but `systemctl is-active` != active, (b) FAIL when sha256(enforced ROBOT.md) != sha256(canonical) |
| **gateway-stop-is-silent** — gateway cannot be stopped silently leaving an ungated robot; no fleet gating-liveness surface (epic gaps 1, 7) | missing | `install_gateway.py:39` unit is `Restart=on-failure` — deliberate stop leaves the robot ungated, no alarm; no watchdog/heartbeat/liveness publication (grep `239.255|6600|multicast|mdns|zeroconf|fleet` over cli/src = zero code hits); doctor never checks unit active-state; `install_gateway.py:92` checks is-active only during install | PLAN: test that doctor (or new `robot-md gating-status`) exits non-zero when the gateway unit is enabled-but-inactive while drivers are gateway-owned; fleet surface = liveness heartbeat asserted against RRF |
| **enforced-vs-canonical-manifest-drift** — the manifest the gateway enforces is provably the manifest the operator authored (epic gap 5) | missing | `install_gateway.py:6,124` copies ROBOT.md into /etc/robot-md-gateway/ at install time; `__main__.py:1787-1797` acknowledges the copy is unreadable by the operator afterward. Nothing on main re-compares; drift is structural: two files, one-shot copy, no checksum, no re-sync command | PLAN: unit test for a doctor drift check (mismatched hashes → FAIL with 're-run install-gateway --sync') |
| **robotmd-carries-p66-manifest** — ROBOT.md can carry or point to a full P66 manifest today | partial | No first-class field (only `compliance.fria_ref` URI, schema:471-475). Only legal carrier = `extensions.x-*` (schema:536-542; spec/robot-md-v1.md:243-256) — but spec mandates unknown x-* keys MUST NOT fail validation (:255), so anything there is by-design unvalidated: carriable, not provable | PLAN: once a first-class `safety.p66` block (or `p66_manifest_ref` URI) lands, schema test rejecting malformed P66 fields — the extensions path can never provide this |
| **hitl-gates-schema-and-enforcement** — safety.hitl_gates declarable and enforced at the agent boundary | implemented | Schema: `schema/v1/robot.schema.json:403-414` (scope required, require_auth :410). Enforcement (MCP Layer 2): `execute_capability.py:91-100` (blocks without confirm_token), scope-prefix matching :136-145; estop_clear gated by system scope (`mcp/tools/estop.py:43-61`, require_auth default-true :51). Caveats: schema doesn't require require_auth; Layer-2 in-process enforcement is bypassable by a shell-capable agent (SECURITY.md:69) — the RCAN-gated version lives in robot-md-gateway | `cli/tests/test_mcp_audit_integration.py:119-134,243-257`; `cli/tests/unit/test_mcp_tools_execute_capability.py:93` — exists |
| **estop-never-blocked** — estop-set ungated, outranks every gate; read-only observability survives; fail-closed without a declared read_only set | implemented | `mcp/tools/estop.py:35-40` (set unconditionally; audit failure suppressed :30-32); `execute_capability.py:52-64` (estop checked FIRST, outranks consent; live flag passed into backend.execute for mid-motion abort :112-114); read-only caps survive estop :55-56. Caveat: only clearing is gated, never setting; no concurrent-abort test yet | `cli/tests/unit/test_mcp_tools_estop.py:7`; `cli/tests/unit/test_execute_capability_readonly.py:27,39,53`; `test_mcp_audit_integration.py:138-150`. GAP-PLAN: concurrent test — estop_tool during in-flight backend.execute must abort within estop.response_ms |
| **fleet-multiactor-invariants** — mDNS discovery, fleet ESTOP UDP 239.255.66.0/24:6600 500ms SLA, federation fields | missing | `discovery.py` is .well-known/robot-md.json + RRF discovery, not mDNS; grep `239.255|6600|multicast|mdns|zeroconf` across cli/src = nothing; schema network block (schema:425-440) has rrf_endpoint/port/signing_alg/transports only | PLAN: after fields land, two-process integration test: node A multicasts fleet ESTOP on 239.255.66.x:6600, node B's MCP context flips estop within 500ms (monotonic clock) |

---

## 4. OpenCastor runtime

Judged at HEAD `38ff96b`. SafetyLayer = `castor/fs/safety.py:74` (not `castor/safety/protocol.py` as that repo's CLAUDE.md claims); P66 manifest = `castor/safety/p66_manifest.py`, served at GET /api/safety/manifest (`castor/api.py:4712`). Headline: strong at the unit/declaration layer, **broken at exactly the seams the epic cares about** — the cloud/fleet ESTOP route mismatch, the arm-endpoint SafetyLayer bypass, and confidence gates declared "implemented" in the manifest while no live call site supplies confidence. Note: OpenCastor is the designated P66 tier but nothing here is wired into Bob's current gated stack.

| Invariant | Status | Evidence | Proving test |
|---|---|---|---|
| **estop-never-blocked-local** — ESTOP always available regardless of state (manifest rule EMERGENCY_001): bypasses HiTL, confidence gates, replay, offline mode, federation | partial | Strong at unit level: `fs/safety.py:444-448,722-739`; `api.py:4654-4709` (/api/safety/rcan bypasses HiTL+confidence); `bridge.py:1350-1352` (replay bypass), :460-463 (offline), :563-572 (federation). **Broken at wiring:** cloud bridge + fleet MCP dispatch ESTOP to POST `{gateway_url}/api/estop` (`bridge.py:1886-1891`; `mcp_fleet.py:61,207`) but api.py defines NO /api/estop route (only /api/stop :795, /api/estop/clear :805, /api/fs/estop :815) — **cloud/fleet ESTOP 404s, while the QoS ACK is written to Firestore BEFORE dispatch** (`bridge.py:1370-1397`), manufacturing a false 'ESTOP acknowledged'. /api/stop + /api/safety/rcan require verify_token — unauthenticated ESTOP rejected 401. Arm path ignores estop: `set_joint_positions` with no is_estopped check (`api.py:5656-5664`, pick_place from :2856); SafetyLayer estop only auto-blocks /dev/motor paths (`fs/safety.py:444`); /dev/arm guard (`hardware/so_arm101/safety_bridge.py:138-142`) is fail-open without a SafetyLayer (:129-136) | Units exist: `tests/test_v15_bridge.py:320-336,362`; `test_v16_bridge.py:76-91`; `test_p66_harness.py:50-137`; `test_safety_invariants.py:248-256`. MISSING: end-to-end test POSTing a bridge-shaped ESTOP doc through a real TestClient(app) — would catch the /api/estop 404 immediately (`tests/test_mcp_fleet.py:59` mocks the URL, masking it) |
| **estop-clear-protected** — explicit authorized clear; remote RESUME cannot silently clear | implemented | `fs/safety.py:763-793` (root/CAP_SAFETY_OVERRIDE + optional OPENCASTOR_ESTOP_AUTH); `api.py:4697-4705` (RCAN RESUME failure surfaces hint, no force-clear) | `tests/test_safety_invariants.py:181-211` (auth matrix), :257-266 (remote /proc/status write cannot clear) — exists |
| **min-loa-for-control** — gates control/system scope commands | partial | Cloud path: `bridge.py:397-398,624-665,686-702`. But Firestore-bridge-only — local HTTP API uses JWT role/scope RBAC (`rcan/jwt_auth.py`) with no LoA concept; manifest **hardcodes** `min_loa_for_control: 1` (`p66_manifest.py:442`) instead of live config (live value only in bridge get_status :966-970) — manifest can drift from enforcement | `tests/test_loa_v3.py` + `test_v16_bridge.py` (bridge-level). MISSING: test that GET /api/safety/manifest reflects configured min_loa_for_control; any LoA test on the local API path |
| **clock-sync-check** — clock_synchronized checked/declared | partial | `_check_clock_sync` exists only as a CLI conformance probe (`cli.py:2822-2850`, invoked :2730). Never consulted at dispatch; build_manifest (`p66_manifest.py:430-460`) has NO clock_synchronized field; replay checks (`bridge.py:492-539`) trust issued_at against local clock unconditionally | MISSING. PLAN: manifest test asserting clock_synchronized present + bridge test that unsynced clock degrades replay-window trust or flags the manifest |
| **offline-mode** — ESTOP-only after grace, local key cache, cross-owner blocking | partial | `bridge.py:58` (OFFLINE_THRESHOLD_S=300, **hardcoded**, no offline_grace_period_s config), :429-486 (ESTOP always allowed; safe-offline whitelist); manifest claims offline_mode_capable (`p66_manifest.py:437`). No local registry key cache (auth is shared-secret HMAC JWT, `rcan/jwt_auth.py:1-19` — nothing to cache); cross-owner blocking only implicit via ESTOP-only filter (consent needs Firestore, `cloud/consent_manager.py:101`). Applies only to the Firestore relay path, not local API | `tests/test_v15_bridge.py:338-365`. MISSING: configurable grace period test + explicit offline cross-owner-deny test |
| **federation-trust** — cross-registry validated vs trust anchors, ESTOP exempt | partial | `bridge.py:546-618` (ESTOP bypass :563-572); BUT `require_federation_trust` defaults False = **fail-OPEN** with the rcan.federation SDK stub (`bridge.py:400-403,597-604`); manifest hardcodes federation_enabled: False (`p66_manifest.py:439`); literal `trusted_registries` config key appears nowhere in the repo (grep: zero) | `tests/test_v16_bridge.py:74-95`. MISSING: fail-closed test with require_federation_trust=True against a real trust-anchor set |
| **confidence-gates-equal** — cloud commands pass the same confidence gates as local (manifest rule SOFTWARE_002 claims 'implemented') | partial | Gate code correct (`confidence_gate.py:80-114`; `fs/safety.py:497-525`) — but **no live command path supplies confidence meta**: `api.py:779` (/api/action), `api.py:5838`, `main.py:1512`, `drivers/base.py:102` all call fs.write without meta; main-loop ConfidenceGateEnforcer constructed then never consulted (`main.py:1078-1087`). Gates equally UNENFORCED for cloud and local — parity by vacuity, contradicting the manifest's 'implemented' claim. Path parity itself real: bridge relays through the same HTTP API (`bridge.py:1837-1905`); write_remote re-uses write() (`fs/safety.py:699-720`) | fs-level tests pass meta directly; nothing proves the brain→fs.write path carries confidence. PLAN: end-to-end test that a low-confidence Thought.action is blocked at /dev/motor on BOTH /api/command and a bridge-relayed command |
| **bounds-checks-equal** — cloud commands pass the same bounds checks/clamping as local | partial | Motor path implemented: `fs/safety.py:546-598` (bounds, protocol rules, rate limit + clamp); routed via `api.py:777-789`, `main.py:1463-1478,1510-1517`; cloud reaches the identical path (bridge → local HTTP API); write_remote runs every check with source='rcan'. **Hole:** /api/arm endpoints bypass SafetyLayer entirely for BOTH local and cloud — `set_joint_positions` direct (`api.py:5656-5664`; pick_place :2856) without write_arm_command; only vla_provider routes joints through it (`providers/vla_provider.py:140,195`); legacy driver move() also bypasses (`drivers/base.py:10-18`) | `tests/test_safety_invariants.py:248-273` (write_remote estop-blocked + clamped). MISSING: test asserting /api/arm/pick_place and pose writes are bounds-checked and estop-gated |
| **p66-manifest-real-fields** — manifest carries real fields, derived from live state | partial | Far richer than robot-md's boolean: `p66_manifest.py:430-460` (replay_cache_enabled, sender_type_logged, offline_mode_capable, federation_enabled, supported_transports, min_loa_for_control, loa_enforcement + 20 per-rule statuses), served at `api.py:4712`. BUT v1.5/v1.6 fields are **hardcoded literals** not live reads (min_loa_for_control:1 at :442; supported_transports ['http'] at :440 despite the MQTT transport in `channels/rcan_mqtt_transport.py`); clock_synchronized absent; conformance_pct credits 7 v1.5/v1.6 invariants as implemented **by fiat** (`p66_manifest.py:407-412`) — asserted, not provable | `tests/test_conformance*.py` exercise shape only. PLAN: test that manifest field values derive from live CastorBridge/config rather than constants |
| **hitl-gates-under-command-path** — destructive/gated actions require HiTL on the live command path (rule PROPERTY_001) | implemented | `api.py:2878-2925` (pick_place HiTL pending/consume via state.hitl_gate_manager); `api.py:5903-5994` (_wire_notify_dispatch builds HiTLGateManager); `main.py:1480-1508` (DestructiveActionDetector + WorkAuthority). Note: main.py-local _hitl_gate_manager is acknowledged dead code (`main.py:1091-1095`); live wiring is api.py | `tests/test_p66_harness.py:153-208` + api endpoint tests — exists |
| **r4-cloud-relay-status** — opencastor.com cloud relay live (last remaining capstone from the 2026-05-29 session) | missing | Relay CODE exists (CastorBridge `bridge.py:1,296`; CLI run_bridge `bridge.py:2340`, `cli.py:3653`; sender_type audit `bridge.py:1328-1366`; consent manager). But no opencastor.com binding anywhere in castor/cloud (grep: only Firestore/Cloud Function refs); repo wrangler.toml is the website; and cloud ESTOP dispatch targets the nonexistent /api/estop route (`bridge.py:1888`) — the relay cannot deliver a working ESTOP even if deployed. No evidence any relay runs for Bob | PLAN: live round-trip test cloud doc → bridge → gateway 200 (not 404) → outcome receipt; today only mocked-httpx bridge unit tests exist |
| **fleet-multiactor-estop** — fleet ESTOP fan-out, mDNS, UDP 239.255.66.0/24:6600, 500ms SLA | missing | No multicast fleet ESTOP: grep 239.255/6600/multicast finds only an SSRF guard REJECTING multicast addresses (`api.py:4251-4252`). Fleet ESTOP is HTTP-only via `mcp_fleet.py:58-61,207` posting to the nonexistent /api/estop route. mDNS module exists (`castor/rcan/mdns.py`) but no UDP safety channel. ESTOP_ACK_DEADLINE_S=2.0s (`bridge.py:61`) vs the spec's 500ms SLA | `tests/test_mcp_fleet.py:59` mocks '/api/estop' as if it existed — the mock masks the missing route. PLAN: fleet ESTOP test against TestClient(app) with latency assertion |

---

## 5. Cross-cutting / multi-actor

These invariants require ≥2 actors, a fleet surface, or a cross-repo seam — no single component can prove them. Status here = "is the invariant provable across the ecosystem today".

| Invariant | Status | Evidence | Proving test |
|---|---|---|---|
| **mDNS local supremacy, proven with ≥2 nodes** (rcan-spec discovery; epic gap 8) | missing | Spec text exists (rcan-spec table above, implemented). Implementations: robot-md = none (discovery.py is RRF/.well-known, not mDNS); gateway = none (grep zero); OpenCastor has `castor/rcan/mdns.py` but no multi-node test anywhere | PLAN: two-node test: registry unreachable → peer still resolvable via `_rcan._tcp.local` and accepts owner ESTOP |
| **Fleet ESTOP UDP 239.255.66.0/24:6600, ≤500ms to 100 robots** (rcan-spec §15; epic gap 8) | missing | Spec partial (SHOULD-only, no wire encoding). Zero implementations: gateway grep zero; robot-md grep zero; OpenCastor's only 239.255 hit is an SSRF guard *rejecting* multicast (`api.py:4251-4252`), and its HTTP fleet ESTOP posts to a route that does not exist | PLAN: multi-robot harness measuring multicast ESTOP receipt latency at n=10 (then 100) + unicast-fallback correctness — the epic says these tests should exist before robot #2 does |
| **federation_enabled / trusted_registries fail-closed across the stack** (rcan-spec §18; epic gap 8) | partial | Spec implemented (federation.astro). OpenCastor bridge has the check with tested ESTOP bypass (`test_v16_bridge.py:74-95`) but defaults fail-OPEN with the SDK stub and never reads a `trusted_registries` key; gateway and robot-md have zero federation code | PLAN: fail-closed test with require_federation_trust=True + real trust anchors (OpenCastor); federation fields in ROBOT.md schema (robot-md); cross-registry deny test at the gateway |
| **HITL gates under multi-actor RCAN (fleet/delegation)** (epic gap 8) | missing | Spec gap: no HITL semantics in §15 FLEET_COMMAND (`messages.astro:378-409`); single-robot HITL is proven at three layers (rcan-spec L3-HITL-001/002; robot-md MCP tests; OpenCastor p66_harness) but nothing tests a fleet command hitting a member robot's gate | PLAN: spec the per-robot queueing rule, then test "FLEET_COMMAND matching a member's HiTL gate is queued per-robot, not auto-dispatched" |
| **Fleet gating-liveness surface — "is every robot gated right now?"** (epic gap 7; PlatAtlas) | missing | No component emits a gating-liveness heartbeat (gateway: grep zero; robot-md: grep zero) and PlatAtlas has no gated/ungated/stale rendering — a robot that stops sending verified actions is indistinguishable from an idle one (epic #96 gap 7; workflow-atlas#205 browser-only ledger, workflow-atlas#206 liveness overlay). PlatAtlas was not separately audited in Phase 0; its status derives from the absence of any emitter | PLAN (Phase 3): gateway heartbeat attestation upstream → PlatAtlas renders gated/ungated/stale per actor; alert-on-silence test; exit = one view answers "are all robots gated right now, and can we prove the last action?" |
| **Bring-up harness: RCAN-on as a timed gate** (workflow-atlas#193 cross-ref) | missing | workflow-atlas#193's ten-minute bring-up gates do not currently include gating: a fresh robot is born ungated and gated later by hand — the exact window the 2026-06-03 incident lived in. The epic's cross-ref is explicit: "its timed gates should *include* RCAN-on as a gate, so a fresh robot is born gated" | PLAN: add an "RCAN-on" timed gate to the #193 harness: bring-up does not pass until the gateway is active+enabled, enforced manifest hash == canonical, and a signed read_state round-trips vs prod RRF — measured inside the ten-minute budget |

---

## 6. What Phase 1/2/3 must flip

Phase definitions verbatim from the epic (robot-md#96 "Proposed program"):

> - **Phase 1 — Always-on enforcement on Bob.** Gap 1 (watchdog + port lockout or attestation), gap 5 (manifest unify), gap 3 (#28 camera parity). Exit: stopping the gateway either fail-closes motion or is visibly alarmed within N minutes; `doctor` is green on manifest sync.
> - **Phase 2 — P66 conformance proven vs prod RRF.** Gap 2 (#27 receipts), gap 4 (schema decision), gap 6 (OpenCastor invariant map), ESTOP-latency measurement under gating. Exit: a regenerated proof artifact (successor to `rcan_proof_2026-05-29.json`) covering command-authorization AND outcome-receipt AND estop, verified against RRF prod keys.
> - **Phase 3 — Fleet evidence surface in PlatAtlas.** Gap 7: gated/ungated/stale per actor + receipt verification status. Exit: an operator (or agent, post workflow-atlas#205) can answer "are all robots gated right now, and can we prove the last action?" in one view.

### Phase 1 — Always-on enforcement on Bob

| Row (component) | Today | What flips it |
|---|---|---|
| always-on-gating-no-silent-stop (gateway) | missing | Restart=always + WatchdogSec/sd_notify in the shipped unit, or fail-closed port lockout, or heartbeat attestation — decided + implemented + CI-checked |
| gateway-stop-is-silent (robot-md) | missing | doctor (or `robot-md gating-status`) exits non-zero on enabled-but-inactive gateway with gateway-owned drivers |
| device-node-isolation GW-001 (gateway) | partial | Remove craigm26 from robot-md-gateway + dialout groups; run live HIL GW-001 (EACCES from operator UID) |
| manifest-drift-detection (gateway) | missing | Receiver pins ROBOT_MD_PATH (reject envelope-supplied manifest_path outside it) or RRN-binding on by default; drift surfaced |
| enforced-vs-canonical-manifest-drift (robot-md) | missing | doctor drift bucket: sha256(enforced) != sha256(canonical) → FAIL; `install-gateway --sync` re-sync path. *In-flight: feat/commission-bringup carries pieces* |
| doctor-commission-and-drift-bucket (robot-md) | missing | Commission + drift + gateway-liveness buckets land on main (in-flight branch merges) |
| actuator-gated-path-parity gw28 (gateway) | missing | Merge + release oak-d-actuator PR #11; gateway-side entry-point signature conformance test; HIL gated vision_find |
| estop-never-blocked (gateway) | partial | Wire SafetyMonitor in production serve; add an estop op to Bob's allowlist so an e-stop path exists through the gated channel; HIL SF-001 latency |
| safe-stop-on-network-loss (gateway) | partial | Wire SafetyMonitor + a heartbeat source in serve; HIL SF-002 (<3.5s) |
| manifest-provenance-no-drift (rcan-spec) | missing | Normative text: enforced_config_hash in P66 manifest + restricted-mode-on-mismatch (spec prerequisite for the doctor/gateway checks above) |
| gating-liveness-never-ungated (rcan-spec) | missing | Normative text: gating_active/gating_attestation fields + "kill enforcement process → safe-stop + fleet flag" conformance row (spec prerequisite for gap 1's attestation option) |
| Bring-up RCAN-on gate (cross-cutting / workflow-atlas#193) | missing | Add the timed RCAN-on gate to the ten-minute harness so a fresh robot is born gated |

### Phase 2 — P66 conformance proven vs prod RRF

| Row (component) | Today | What flips it |
|---|---|---|
| outcome-receipts gw27 (gateway) | partial | Deploy the attestation build to Bob + set ROBOT_MD_ATTESTATION_*; add signed receipt to the /v1/invoke RESPONSE; close gateway#27 |
| signed-outcome-receipts (rcan-spec) | partial | Spec the RECEIPT field on RESPONSE/COMMAND_ACK + conformance row (spec prerequisite for #27 being conformant rather than bespoke) |
| p66-manifest-endpoint (rcan-spec) | partial | Publish `schemas/safety-manifest.json` + validate the v1.6 example against it |
| p66-real-manifest-fields (robot-md) | missing | The epic's gap-4 schema decision: safety.p66 object in ROBOT.md (or p66_manifest_ref pointer) — one schema-validated source of truth |
| p66-claim-not-provable (robot-md) | missing | `doctor --strict` corroborates p66_enabled against a live gateway P66 probe |
| robotmd-carries-p66-manifest (robot-md) | partial | Same gap-4 decision; first-class field replaces the unvalidatable extensions path |
| loa-gates-p66 (gateway) | partial | Map min_loa_for_control → tiers; manifest-declared P66 fields change gateway enforcement, with test |
| clock-sync-p66 (gateway) | missing | Envelope timestamp-window + clock-sync probe in doctor/commission; stale-clock-denies test |
| offline-mode-p66 (gateway) | missing | Wire RevocationResolver in serve; bounded key-cache staleness; fail-closed offline policy test |
| replay-protection (gateway) | partial | Persistent replay cache (sqlite) + across-restart test + HIL tail-replay |
| cloud-cmds-same-confidence-gates (gateway) | partial | ROBOT_MD_CONFIDENCE_* env wiring in serve + deployed-config test |
| hitl-gates-enforced-under-rcan (gateway) | partial | Set ROBOT_MD_HITL_FROM_MANIFEST on Bob + deployed-config conformance check |
| audit-evidence-chain EV-001 (gateway) | partial | Persistent chain across restart + full-export endpoint |
| security-md-tier2-enforcement-claim (robot-md) | partial | The epic's gap-6 OpenCastor invariant map (delivered: §4 of this doc) graduates to a cross-repo conformance test + Tier-2 badge |
| All OpenCastor partial rows (§4: estop-never-blocked-local, min-loa, clock-sync, offline, federation, confidence-gates, bounds-checks, manifest-real-fields) | partial | Fix the /api/estop route mismatch (first prove-it test: TestClient bridge-shaped ESTOP → expect 200, fails today with 404); gate /api/arm/* through SafetyLayer; wire confidence meta into live fs.write paths; derive manifest values from live config; stop crediting conformance_pct by fiat |
| local-safety-wins (rcan-spec) | partial | Fix schemas/rcan-config.json (safety.required must include local_safety_wins) + failing-validation test |
| cloud-commands-same-gates (rcan-spec) | partial | Normative "identical gate set regardless of ingress" section + gate-parity-by-ingress L2 rows |
| estop-explicit-clear / estop-never-blocked / clock-sync / offline / qos2 / min-loa / federation / constrained-transport / queue-bypass / ai-cannot-override (rcan-spec) | implemented (text) | Promote the plan-only acceptance tests into the published L1–L4 conformance suite — Phase 2's "ESTOP-latency measurement under gating" lands here (HIL SF-001) |
| r4-cloud-relay-status (OpenCastor) | missing | Live round-trip cloud doc → bridge → gateway 200 → outcome receipt (R4 capstone; depends on the route fix) |

**Phase 2 exit artifact:** regenerated proof (successor to `rcan_proof_2026-05-29.json`) covering command-authorization AND outcome-receipt AND estop, verified against RRF prod keys.

### Phase 3 — Fleet evidence surface in PlatAtlas

| Row (component) | Today | What flips it |
|---|---|---|
| Fleet gating-liveness surface (cross-cutting) | missing | Gateway heartbeat attestation → PlatAtlas gated/ungated/stale per actor + receipt verification status (workflow-atlas#205/#206) |
| always-on-gating (gateway, attestation half) | missing | The "visible within minutes" leg of gap 1 — the emitter Phase 3 consumes |

### Unphased in the epic — must exist before robot #2 (gap 8)

The epic assigns no numbered phase to multi-actor conformance, only the constraint that "the conformance tests should exist before robot #2 does": fleet-estop-500ms-sla (rcan-spec, partial — needs wire encoding + measurement method first), fleet-multi-actor-p66 (gateway, missing), fleet-multiactor-invariants (robot-md, missing), fleet-multiactor-estop (OpenCastor, missing), mdns-local-supremacy two-node proof (cross-cutting, missing), hitl-gates under fleet (cross-cutting, missing), federation fail-closed (cross-cutting, partial).

---

## 7. Proven baseline — do not re-prove

Carried verbatim from the epic. These are done; Phases 1–3 build on them, they are not re-run.

- **Signed-invoke chain vs PROD RRF** (2026-05-29, Bob/RRN-000000000011): operator-signed RCAN INVOKE → robot-md-gateway → execute; gateway verifies manifest-provenance footer + envelope sig live against RRF `/v2/keys`; tampered envelope / rogue kid → 403 fail-closed.
  - Proof artifact: `~/castor-dash/rcan_proof_2026-05-29.json` (verified present 2026-06-10)
  - Signer tooling: `~/opencastor-ops/scripts/hil/`
  - Operator key: `bob-operator-2026`, held at `~/.opencastor-ops-keys/bob-operator.priv.pem`, RRF-registered (note: `/v2/keys` is keyed by OPERATOR kids, not robot pq_kids — robot-kid 404 is by-design)
- **Live telemetry into PlatAtlas** (2026-06-09/10): signed samples + camera recovery, gateway-mediated; PlatAtlas actor graph populates from cryptographically verified RCAN records only (`binding_ok=true`).
- **Gateway service on Bob**: currently `active` + `enabled` (re-enabled after the 2026-06-03 stop incident; the incident itself is the motivating evidence for Phase 1, not part of the baseline).

What the baseline does NOT cover (why Phases 1–3 exist): outcomes are not provably executed-as-authorized (no receipts), the gateway proving the baseline can be silently stopped, the manifest it enforces has drifted from canonical, the camera fails through the gated path, and ESTOP has no path through the gated channel at all.

---

*Phase 0 of robot-md#96. Investigations run 2026-06-09/10 against merged main of each repo; component evidence cited per row. Maintained in robot-md `docs/CONFORMANCE-P66.md`.*

[robot-md#96]: https://github.com/RobotRegistryFoundation/robot-md/issues/96
